# Estado del proyecto: planta-hidrogeno

- El documento maestro se mantiene en `planta-hidrogeno.tex`.
- El contenido se organiza por capítulos dentro de `capitulos/`.
- La salida de compilación se conserva en `build/` para no romper la automatización del HTML.
- La clase y la plantilla compartida no se modifican.
